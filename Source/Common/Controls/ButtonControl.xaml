<UserControl x:Class="Common.Controls.ButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="26" d:DesignWidth="300" Height="26">
    <Button Command="{Binding Command}" CommandParameter="{Binding CommandParameter}">
        <StackPanel Orientation="Horizontal">
            <Image Stretch="Uniform" Margin="2" Source="{Binding Image}"></Image>
            <TextBlock Margin="2" Text="{Binding Text}"></TextBlock>
        </StackPanel>
    </Button>
</UserControl>
